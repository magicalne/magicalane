# magicalane test-env: TCP+TLS transport client (TLS enabled)
password = "test-password"
bandwidth = 65536
verbose = true
kind = { Client = { proxy = { host = "magicalane-server", port = 4535, ca_path = "/etc/magicalane/certs/ca.pem", protocol = "tcp" }, socks5_port = 1080, tproxy = { tcp_port = 7895, udp_port = 7896 } } }
